import { Client } from 'discord.js';

export function initializePeriodicUpdate(client: Client) {
  // initialize the scoreboardUpdate
  //setInterval(() => void scoreboardUpdate(client), 1000 * 60 * 2);
}
